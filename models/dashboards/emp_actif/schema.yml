version: 2 

models:
  - name : report_emp_active 
    description: >
        Table uniquement pour les employés actifs qui sont actifs actuellement
    tests: 
      - resolution: 
          combination_of_columns: 
            - matricule
            - annee                 
            - today_is_active
            - today_job_categories
            - stat_eng
            - corp_empl_descr
